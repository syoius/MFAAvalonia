# P7＋P8 本仓库部分联合阶段审查

审查日期：2026-09-22。分支 `integration/desktop-p0-p3-20260919`，工作树 `C:\MFAAvalonia\.worktrees\desktop-p0-p3`。审查范围为已接受的 P6 基线 `558be64bf63b46b6ea46d4a4a659d52f0720a83d` 到 `2040b7fb25042d76f991b6dbbb65e618dd32cdd9`，包括 P7 `e6585e6` 和 P8 `2040b7f`。

**结论：需要一项限定返修，仅涉及既有回归测试的日期依赖与验证记录。P7/P8 生产改动本次未发现需要阻断交付的实现缺陷；联合阶段暂不标为全部通过，不进入 P9。** 此结论不重开 P5、P6 已关闭的生产修复，也不要求重做已接受功能。

## P7/P8-R1：启动测试使用已经过去的定时时间，完整回归入口失败

性质：确认的测试缺陷，属于本机验收证据修复，不是已经确认的产品定时回归。

- 位置：集成工作树 `tests/DesktopStartup.Tests/Program.cs:188` 固定 `dueAt = new DateTime(2026, 9, 20, 10, 0, 0)`，下一行对生产投影结果调用 `Single()`。
- 实际触发：正常运行 `./scripts/test-desktop-compatibility.ps1 -NoRestore`。该测试前面的迁移成功路径会执行 `DesktopMigration.Run`，在 `MFAAvalonia/Configuration/Compatibility/DesktopMigration.cs:85` 将 `SuppressTimersThrough` 设为真实当前时间。
- 原因：`MFAAvalonia/ViewModels/Other/TimerModel.cs:238` 正确过滤迁移抑制时间以前的事件。2026-09-22 运行时，9 月 20 日的固定事件已过期，投影为空；“current target”和“named target”两组均在第 189 行抛出 `Sequence contains no elements`。
- 影响：正式脚本返回 1，并停止后续项目执行；不能把历史的 130 PASS 直接当作今天可复现的完整通过结论。交付记录保留的 9 月 20 日测试结果不因此被否认，但需补上本次修正后的验证。

限定修复：让测试使用迁移抑制时点之后、满足其原有星期规则的确定性测试时间，并让该用例的模拟运行时使用同一时点；或在已有测试边界内统一可控时钟。保留改绑后的来源/目标、周计划、持久化和实际调度断言。不要继续换一个更远的硬编码日期，不要删除 `Single()`/执行断言来让结果变绿，也不要为此放宽生产迁移的定时抑制规则。

验收：修正后从正式脚本完整运行四个项目，确认原 130 组均通过并记录日期和退出码。若只改测试和文档，无需重复未变的双 RID Desktop build/publish/ZIP，也不需要新增迁移或时钟框架。返修后的独立复审仅核对此项、涉及的调用及最新结果。

## 本次确认的实现

P7 的主任务进度读取实际运行集合并绑定 Processor/runId，独立 Copilot 的重复、失败、取消与清理占用没有退回首页队列计数。实例可编辑性与全局诊断设置分别使用实例/全局状态；最终执行和录制释放能更新状态。此前 P6 留给 P7 的 Root.IsRunning 收尾、AfterTask 有效值显示同步已补齐，保留真实用户编辑的写入版本保护。

布局写入绑定页面实例与配置代际；命名配置往返及旧回调保护有正式用例。总控台借用 LiveView 图片，隐藏/卸载时解除引用和订阅，没有把消费者当作图片所有者释放。实际 Monitor XAML 的按钮、祖先隐藏、恢复和卸载测试通过；查看了本次 headless 渲染图，竖屏卡片与内嵌启停可见。该证据不替代实际设备画面和原生窗口体验。

P8 的品牌 apphost、程序集依赖、双 RID GUI ZIP、Release/Test/Mirror 文件匹配与 tag 元数据保持一致。独立交付检查使用现有真实候选 ZIP，通过文件集合、架构、Windows 品牌入口与用户数据排除检查。工作流只构建 Desktop；发布 job 不依赖 Android/APK。真实 runner 和远程发布步骤未执行。

普通资源检查仍使用 interface 指定的 MaaYuan 仓库/RID及资源版本，独立 GUI 更新保持隐藏；匹配指定平台/架构的最终包与通用资源包有生产路径测试。新增文件保护覆盖配置、作业、录制、布局等实际数据路径，完整清理、增量删除、复制、失败回滚与品牌入口识别有隔离生产路径证据。未执行真实安装替换和重启。

## 独立验证与证据

所有日志路径以下均相对集成工作树。

| 检查 | 本次结果 | 证据 |
|---|---|---|
| 正式脚本 `./scripts/test-desktop-compatibility.ps1 -NoRestore` | 47 PASS、2 FAIL，退出 1；在 DesktopStartup.Tests 停止 | `artifacts/p7-p8-review-20260922/tests.log` |
| 单独续跑 DesktopResourceLoading.Tests | 81 PASS、0 FAIL，退出 0；覆盖 P7/P8 及共享运行回归 | `artifacts/p7-p8-review-20260922/resource-tests.log` |
| 合并四项目结果 | **128 PASS、2 FAIL**；两项均为上面的日期依赖 | 上述两份日志，未重复计算 |
| Python 交付检查，包含实际候选 | **4/4 通过，无跳过**，退出 0 | `artifacts/p7-p8-review-20260922/delivery-tests.log` |
| 实际 Monitor headless 渲染 | 已生成并查看 | `tests/DesktopResourceLoading.Tests/bin/Release/net10.0/win-x64/p7-monitor.png` |

独立续跑命令：

```powershell
dotnet run --project tests/DesktopResourceLoading.Tests -c Release -r win-x64 -p:MSBuildEnableWorkloadResolver=false -p:UsedAvaloniaProducts= --no-restore
$env:P8_PUBLISH_ROOT='artifacts/p8-20260920/final/publish'
$env:P8_PACKAGE_ROOT='artifacts/p8-20260920/final/packages'
python -B -m unittest discover -s tests/DesktopDelivery.Tests -v
```

已检查交付者现有 `artifacts/p8-20260920/build-win.log`（0 错误、12 警告）、`build-osx.log`（0 错误、13 警告）和两份 publish 日志。本轮未重复构建或打包。Windows 日志的 12 个警告不撤回历史依赖警告；本次测试仍输出既有 NuGet/代码警告。macOS 仍只证明 Windows 交叉构建/包结构。

候选版本为 `v2.14.0-candidate.p8.20260920`。候选在 P8 提交前构建、元数据保留 P7 的提交后缀，交付报告已明确说明；不将其误报为只包含 P7，也不把本次包结构检查写成提交后重新构建。

## 边界与下一步

下一轮仅交付 P7/P8-R1 的测试与证据返修、本地提交及限定复审；无需重写 P7/P8 生产功能。修复完成后可据明确结果关闭联合本机阶段审查。

完整 G8 继续等待 MaaYuan 下游组装的两平台最终包、真实旧包经旧更新器替换到新 GUI 迁移的全链路，以及 Apple Silicon 原生运行。真实系统动作、设备/Agent/窗口、历史样本、干净 runner、满盘/ACL及故障恢复等既有外部验收仍保留，不当作本轮新发现的实现缺陷。

本次只审查、运行隔离验证并在原工作区写此报告；未修改生产代码、测试源码或集成交付文档，未暂存/提交，未进入 P9，未推送/发布或操作下游与 `C:\MaaY`。审查结束时集成工作树仍干净。

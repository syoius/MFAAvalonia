# P6 阶段审查

**最新结论（2026-09-20，HEAD `558be64`）：P6-R1 / P6-R2 均已关闭，P6 本机实现阶段通过，可交接 P7。独立完整回归 117 PASS / 0 FAIL；外部验收仍保留，非发布批准。P7 应一并处理文末记录的界面显示同步问题。下方初审及返修历史保留，以文末最新结论为准。**

审查日期：2026-09-19。工作树：`C:\MFAAvalonia\.worktrees\desktop-p0-p3`；分支：`integration/desktop-p0-p3-20260919`；HEAD：`b7c2b5ce25a088bb1e36a6d0202d8de9023d1fcf`。审查范围为 `5d9843b..b7c2b5c`，包括实际完成入口、全局协调、配置持久化、定时/CLI 调用方和最终执行适配。集成工作树干净。

**结论：P6 需要一项限定返修，暂不进入 P7。** 正式回归独立复跑 **112 PASS、0 FAIL，退出码 0**，双 RID 交付构建日志均为 0 错误、13 警告；这些通过结果不覆盖下述已复现的用户设置回滚问题。P5-R1/R2 保持关闭。

## P6-R1 / P2：提交失败会覆盖用户后来保存的 None，重新启用一次性关机

位置：集成工作树 `MFAAvalonia/Helper/AutomaticAfterTaskCoordinator.cs:123`，尤其第 125 行；相关条件写入为 `MFAAvalonia/Configuration/InstanceConfiguration.cs:123`。

支持路径与触发顺序：

1. 实例使用 `ShutDownOnce` 正常完成，全局占用释放，协调器在提交边界把该来源配置的标记准备为 `None`。
2. 最终执行器正在异步等待系统命令确认。生产代码确实有 `WaitForExitAsync`，最长等待 10 秒，UI 线程不被该等待占住。
3. 用户在结束操作设置中主动选择“无后续操作”（`None`）。实际设置入口 `StartSettingsUserControlModel.OnAfterTaskChanged` 第 175 行调用实例配置的 `SetValue`；该值已经保存成功。
4. 系统命令返回拒绝。回滚只检查有效值是否仍为 `None`，无法区分自身的准备写入和用户后来明确保存的 `None`，将其恢复成 `ShutDownOnce`。

实际影响：用户已取消将来的自动关机，但磁盘配置重新启用一次性关机，后续运行完成仍可能产生关机请求。这不是外部系统验收缺口，而是本机可确定的设置覆盖缺陷。

隔离复现经过真实 Processor 队列/完成入口、协调器和实例文件写入，只替换设备工作及最终系统提交。观察结果：

```text
OBSERVED prepared marker=None
OBSERVED after user saves None and command rejects: diskAfterTask=ShutDownOnce
```

限定修复建议：回滚应确认该来源配置的 AfterTask 仍属于本次准备写入，后续用户保存（包括与准备值相同的 `None`）必须使这次回滚失效。可在现有实例配置写入边界记录这一字段的写入身份，不需要通用事务或迁移框架。不要仅以整个配置发生任意写入来判定失效：最终执行器的 `PersistRuntimeState` 会保存其他字段，不能因此把所有正常拒绝后的标记恢复都跳过。

回归应覆盖用户后来保存 `None` 后的拒绝/异常结果，同时保留已有“无用户修改时恢复一次性标记”“后来保存其他动作不覆盖”“来源配置切换不清错对象”“失败不自动重试”的断言。测试继续使用系统动作替身，不能执行真实关机。

## 已确认正确及审查边界

- 真实完成入口按 runId 处理成功/失败、仅启动和手动取消；设备替身已不再绕过 AfterTask 决策。没有发现需要重开 P5 返修的回归。
- 全局准入与占用判断使用同一锁边界，等待实例实际运行完成、资源租约、录制和重启交接。最后释放者为 None 运行、资源租约或录制时能重新评估请求。
- 普通强制定时、跨配置调度和 CLI 重启交接的本机证据有效；CLI 成功准入后的显式退出与自动 AfterTask 分开处理。
- 优先级、同级稳定顺序、批次执行一次、目标快照和来源配置定位的现有测试通过。明确拒绝不自动重试，Unknown 不冒充未提交；外部系统效果仍未验收。
- 提交失败时保留后来保存的 `RestartPC` 是正确的，但现有测试没有覆盖用户保存值恰好等于准备值 `None` 的情况，不能据此推出所有用户编辑都被保留。

另有一项 P7 可沿用的界面状态观察：实际运行收尾后 `RootViewModel.IsRunning` 仍可为 true，而实例 VM 已为 false；队列事件发生在真实清理之前，最终释放没有刷新该全局属性。隔离延迟清理已复现，代码在 `MaaProcessor.cs:486` 及运行最终释放处。该更新逻辑在本轮前已存在，属于后续全局进度/界面状态检查的具体输入，不扩大本轮 P6 返修范围。

**没有把上述界面状态判为“自动退出被确认框阻断”。** 对本机安装的 Avalonia 11.3.12 使用实际运行的 headless desktop lifetime 核对：`Shutdown()` 在 Closing 处理器设置 Cancel 后仍关闭窗口，记录为 `closing=1, closed=1, visible=False`。因此该阻断猜测不成立，不要求为此改写公共退出语义。普通未启动 lifetime 的试验不提供关闭语义证据，以实际启动 lifetime 的结果为准。

## 验证证据和后续边界

正式回归命令：

```powershell
./scripts/test-desktop-compatibility.ps1 -NoRestore
```

结果为 **112 PASS、0 FAIL，退出码 0**。日志：集成工作树 `artifacts/p6-20260919/review-tests.log`。已读取 `artifacts/p6-20260919/build-win.log`、`build-osx.log`，均为 0 错误、13 警告；未重复执行双 RID Desktop 构建，macOS 仍仅为交叉构建。

隔离探针源码及项目：`artifacts/p6-review-20260919/Program.cs`、`Review.csproj`，复用既有 P4 fixture，输出和配置均位于该忽略目录。行为观察日志为 `probe-final.log`；实际 desktop lifetime 日志为 `lifetime.log`。探针是审查复现，未计入 112 组正式测试。命令：

```powershell
dotnet run --project artifacts/p6-review-20260919/Review.csproj -c Release --no-restore -p:MSBuildEnableWorkloadResolver=false -p:UsedAvaloniaProducts=
dotnet run --project artifacts/p6-review-20260919/Review.csproj -c Release --no-restore -p:MSBuildEnableWorkloadResolver=false -p:UsedAvaloniaProducts= -- --lifetime-only
```

首次探针恢复沿用 `artifacts/desktop-sync-repair2-20260919/NuGet.Config` 和 `C:/.NuGet`。实验中一次不允许的初始化后 lifetime 设置失败保留在 `probe.log`；修正测试初始化后复现退出码为 0。没有修改生产源码、正式测试或依赖，没有启动正式 GUI、关闭用户程序或执行系统关机/重启。

下一轮仅处理 P6-R1、补正式回归、更新 P6 交付并本地提交，再做限定复审。交付文档顺带纠正 `桌面端同步进度-2026-09-19.md` 文末仍称 P5“停在独立复审”的旧句，统一为已通过；不用重新审查 P5。

真实系统动作、设备/Agent/原生窗口、Apple Silicon 原生运行、真实历史样本、干净 runner、满盘/ACL/断电及旧 MaaYuan 整包升级等外部缺口继续保留。完成本项本机修复也不等于发布验收通过。

本轮只新增原工作区本报告及集成工作树忽略目录中的审查证据；未修改集成跟踪文件，未暂存、提交、推送、发布或进入 P7～P9，未操作 `C:\MaaY`。原有文档保留。

## 限定复审（2026-09-20）：R1 关闭，新增一项返修回归 R2

审查对象为同一集成工作树和分支，HEAD `1508110db0e8b67031056c0b49ece707fd74b2eb`，工作树干净；限定检查 `b7c2b5c..1508110` 的写入版本、协调器恢复、新增测试和相应配置调用方。

**P6-R1 原缺陷已正确修复。** 显式保存同值 `None` 会推进来源配置的 AfterTask 写入版本，拒绝或异步异常不再覆盖用户选择；没有 AfterTask 写入时，`Instances.PersistRuntimeState()` 保存其他字段不会阻止恢复；编辑另一来源配置也不影响原来源。版本留在内存，未改变磁盘格式。正式回归独立复跑 **115 PASS、0 FAIL，退出码 0**，既有断言保留。

### P6-R2 / P2：实例切换的设置刷新使无用户修改的回滚失效

相关位置：集成工作树 `MFAAvalonia/Configuration/InstanceConfiguration.cs:108`、`:125`；生产调用方 `MFAAvalonia/Helper/Instances.cs:595` 与 `MFAAvalonia/ViewModels/UsersControls/Settings/StartSettingsUserControlModel.cs:173`。

触发顺序为正常多实例操作：

1. 已加载结束操作设置界面，A 使用 `ShutDownOnce`，B 使用另一动作（复现中为 `CloseMFA`）。A 完成，协调器准备把其标记写成 None，正在等待最终命令确认。
2. 用户仅切换 A → B → A 标签，没有更改结束操作。`InstanceTabBarViewModel.SwitchToInstance` 调用 `SwitchCurrent` 和 `ReloadConfigurationForSwitch(false)`，后者第 595 行用当前配置值刷新 `start.AfterTask`。
3. 刷新后的属性变化进入 `OnAfterTaskChanged → SetValue`，将准备值 None 再写回 A。新加的版本记录把这次自动同步也视为新写入。
4. 最终命令拒绝，协调器因版本不同跳过恢复；A 的磁盘值仍为 None。用户并未取消一次性关机，提交也没有成功，该标记却被消耗。

这是返修新增的行为回归：只比较有效值的旧实现会恢复；新增版本检查需要区分真实编辑和从配置读取到界面的同步。不能把所有同值保存都忽略，否则原 P6-R1 会重新出现。

隔离探针使用真实 Processor 完成入口、协调器、实例文件、`MaaProcessorManager.SwitchCurrent`、`Instances.ReloadConfigurationForSwitchAsync(false)` 及实际 StartSettings 属性 setter；只跳过无关页面订阅初始化，替换设备和最终系统动作。没有直接写入待回滚的 AfterTask，也没有修改私有版本表。观察结果：

```text
OBSERVED prepared: disk=None, settings=ShutDownOnce
OBSERVED after A-B-A settings refresh, no user AfterTask edit: disk=None, settings=None
OBSERVED after rejection: disk=None, reserved=False, calls=1
```

应有结果是最后一行 `disk=ShutDownOnce`。该问题有生产 UI 同步调用链和本机复现，不属于设备、Apple Silicon 或真实关机的外部验收缺口。

限定修复：让配置 → 设置界面的 AfterTask 刷新只更新显示，不再次持久化该字段、不推进用户写入版本；真实用户选择仍经过现有写入版本边界。不需要重构配置系统或修改全局结束协调策略。补充通过实际设置刷新路径的 A → B → A 回归，确认拒绝时恢复标记；保留真实用户选择 None、异步异常、另一来源配置编辑和普通运行字段保存的已有断言。

### 本次验证与交接

- 正式完整回归仅独立复跑一次：`./scripts/test-desktop-compatibility.ps1 -NoRestore`，**115 PASS / 0 FAIL，退出码 0**。日志为集成工作树 `artifacts/p6-r1-20260919/review-tests.log`。
- 已核对返修 `build-win.log`（0 错误、1 警告，增量构建）、`build-osx.log`（0 错误、13 警告，交叉构建）和 `restore-win.log`。没有重复双 RID Desktop 构建；低增量警告数不代表历史告警消失。
- 定点复现源码与日志位于忽略目录 `artifacts/p6-r1-review-20260920/`：`Review.csproj`、`Program.cs`、`probe.log`。首次以既有离线 NuGet 配置及 `C:/.NuGet` 恢复，退出码 0。可用 `dotnet run --project artifacts/p6-r1-review-20260920/Review.csproj -c Release --no-restore -p:MSBuildEnableWorkloadResolver=false -p:UsedAvaloniaProducts=` 重现。它是审查探针，不计入正式 115 组测试。

**阶段结论：P6-R1 关闭，P6 因 P6-R2 需要一项限定返修，暂不进入 P7。** 后续只处理上述 UI 同步写回边界与回归；不重开 P5，也不重复已排除的自动退出阻断猜测。P7 的全局 IsRunning 状态观察和全部外部验收缺口继续保留。

本轮仅更新原工作区本报告并写入忽略目录中的验证证据；没有修改生产代码、正式测试或集成交付文档，没有暂存、提交、推送或发布，未操作 `C:\MaaY`，集成工作树保持干净。

## R2 限定复审（2026-09-20）：P6 本机实现阶段通过

审查对象：同一集成工作树及分支，HEAD `558be64bf63b46b6ea46d4a4a659d52f0720a83d`，工作树干净。本轮限定检查 `1508110..558be64` 的配置到界面刷新、真实用户编辑路径、正式测试及交付证据，没有重开已通过的执行协调或 P5 修复。

**结论：P6-R2 正确修复，P6-R1 保持关闭；P6 本机实现阶段通过，可以交接 P7 开发。本轮未实施 P7。** 实际配置与回滚契约通过，不代表原生界面、真实系统动作或最终包验收已经完成。

- `Instances.ReloadConfigurationForSwitchAsync` 改用 `RefreshAfterTaskFromConfiguration()`；后者更新 backing field 并发送 `AfterTask` 属性通知，绕过生成 setter 的保存回调。读取当前实例配置仍正确，自动刷新不再推进 AfterTask 写入版本。
- 正常 `AfterTask` setter 仍调用 `OnAfterTaskChanged → SetValue`。用户真实选择的 None 和其他动作继续受到 R1 的版本保护；没有忽略全部同值写入，也没有改变磁盘格式或协调器策略。
- 新增正式测试经过真实完成、协调、实例切换和设置刷新路径，验证 A → B → A 的显示通知、拒绝及异步异常时恢复来源标记、B 的配置不变，以及刷新后的真实用户编辑仍能保存。两条路径均保留实际 `PersistRuntimeState()`、准入释放和不自动重试断言。

本轮独立完整运行一次 `./scripts/test-desktop-compatibility.ps1 -NoRestore`，**117 PASS、0 FAIL，退出码 0**。日志：集成工作树 `artifacts/p6-r2-20260920/review-tests.log`。已读取本次 `build-win.log`、`build-osx.log` 和 `restore-win.log`：双 RID 均为 0 错误、13 警告，Windows assets 已恢复；macOS 仅交叉构建。本轮未重复双 RID 构建，也未重复运行已经被新增正式测试覆盖的旧探针。

### P7 交接：两项界面状态同步

此前已记录的 `RootViewModel.IsRunning` 收尾滞后继续留给 P7，不将其重新解释为自动退出被拦截。

本轮静态检查另确认：在 A → B → A 已显示准备值 None 后，失败恢复通过 `InstanceConfiguration.TryChangeAfterTask` 把实际配置改回 ShutDownOnce，但 `AutomaticAfterTaskCoordinator` 的恢复分支没有通知已打开的 StartSettings；其属性只在后续配置刷新时重读，因此当前控件可继续显示 None。对应位置为 `AutomaticAfterTaskCoordinator.cs:125`、`InstanceConfiguration.cs:148`、`StartSettingsUserControlModel.cs:178`。现有测试已验证恢复后的内存和磁盘值，但没有声称恢复后的控件显示也已验收。

这一显示与有效配置不一致会误导用户对后续运行行为的理解，P7 交付应处理：恢复后若显示的实例及来源配置仍匹配，刷新 AfterTask 的实际值，使用本轮的不写回刷新路径；不得刷新错实例或覆盖后来的用户选择。归入 P7 的依据是实际标记恢复、用户写入保护和执行决策已经正确，剩余工作是界面状态呈现；不因此再次修改本轮已通过的持久化规则。它不是可忽略的美化项，也不能带着未解决状态宣称最终用户验收通过。

下一实施任务可据本节同步集成文档中的“待限定复审”为已通过，保留历史记录。真实系统动作、Apple Silicon 原生、设备/Agent/窗口、历史样本、干净 runner、故障环境及旧 MaaYuan 整包升级缺口继续保留。

本轮只更新原工作区本报告和忽略目录中的独立回归日志，没有修改生产代码、正式测试或集成交付文件，没有暂存、提交、推送或发布，未操作 `C:\MaaY`。集成工作树保持干净，原有文档保留。
